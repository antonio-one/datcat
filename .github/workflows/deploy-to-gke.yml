name: Deploy to Kubernetes

on:
  push:
    branches: [ main ]

jobs:

    deploy:

        name: Setup Gcloud Account
        runs-on: ubuntu-latest
        env:
#          IMAGE_NAME: gcr.io/${{ secrets.GCP_PROJECT_ID }}/${{ secrets.GCP_APP_NAME }}
          IMAGE_NAME: hub.docker.com/repository/docker/antonioone/datcat
        steps:

        - name: Login
          uses: GoogleCloudPlatform/github-actions/setup-gcloud@master
          with:
            GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
            service_account_email: ${{ secrets.GCP_EMAIL }}
            service_account_key: ${{ secrets.GCP_CREDENTIALS }}

        - name: Configure Docker
          run: gcloud auth configure-docker --quiet

        - name: Checkout repository
          uses: actions/checkout@v2

        - name: Build Docker image
          run: docker build . -t $IMAGE_NAME

        - name: Create .env file
          uses: SpicyPizza/create-envfile@v1
          with:
            envkey_SCHEMAS_PATH: "catalogue/schemas"
            envkey_METADATA_PATH: "catalogue/metadata"
            envkey_MAPPINGS_FILEPATH: "catalogue/mappings/schema_topic_subscription.json"
            envkey_CATALOGUE_SCHEME: "http"
            envkey_CATALOGUE_HOST: "0.0.0.0"
            envkey_CATALOGUE_PORT: 8081
            envkey_CATALOGUE_DEBUG: false
            directory: .test
            file_name: .env

        - name: Test Docker image
          run: docker run $IMAGE_NAME --env-file .test/.env

        - name: Push Docker image
          run: docker push $IMAGE_NAME

        - name: Deploy Docker image
          run: gcloud run deploy ${{ secrets.GCP_PROJECT_ID }} --image $IMAGE_NAME --region europe-west2-c --platform managed
